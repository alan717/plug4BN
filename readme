connect_vscode_debugger(port=12345)



install_api可以安装binninja py库。

connect_vscode_debugger(port=12345)


import modulename 
import importlib


importlib.reload(modulename) #Note this takes an actual module not a string of the name
modulename.handle_address(be, here)